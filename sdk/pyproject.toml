[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agent-os-sdk"
version = "0.1.0"
description = "Agent OS Skill SDK — build skills for Agent OS"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["agent_os_sdk"]
